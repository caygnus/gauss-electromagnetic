/**
 * Types Index
 *
 * Central export point for all shared types
 */

export type { Result } from "./result"
export { success, failure, isSuccess, isError } from "./result"

export type { ContactFormData } from "./contact"

export type { AppConfig, ClientConfig, ServerConfig, EnvConfig } from "./config"
